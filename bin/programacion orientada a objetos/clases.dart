void main() {
  int numero = 10;
  String cadena = "numero";
  bool verdad = true;
  print(cadena+numero.toString()+verdad.toString()); 
}

class Objeto {
  // propiedades
  // ej. color, tamaño, etc...

  // metodos
  // ej. mostrar, crear, etc...
}